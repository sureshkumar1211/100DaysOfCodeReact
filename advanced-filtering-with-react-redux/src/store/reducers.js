import { SORT_BY_ALPHABET, SORT_BY_PRICE, LOAD_DATA, FILTER_BY_PRICE } from "./actions";

const initialState = {};

export const reducer = (state = initialState, action) => {
	switch (action.type) {
		case SORT_BY_ALPHABET: {
		}
		case SORT_BY_ALPHABET: {
		}
		case SORT_BY_ALPHABET: {
		}
		case SORT_BY_ALPHABET: {
		}
		default:
			return state;
	}
};
